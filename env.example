# Django Secret Key
# Generate a new secret key for production
SECRET_KEY=QriRraNsJp74gBTqPA3yYULdXsJEheiNXvfhtIWdgvpZ9gcpjpdqwX9tlGz-csvkmB0

# Debug mode (set to False in production)
DEBUG=True

# Allowed hosts (comma-separated)
ALLOWED_HOSTS=127.0.0.1,localhost,testserver

# Email Configuration
# Using SMTP backend for sending credentials
# IMPORTANT: Gmail requires App Password (not regular password)
# See GMAIL_SETUP_GUIDE.md for detailed instructions
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=YOUR_16_CHARACTER_APP_PASSWORD_HERE
DEFAULT_FROM_EMAIL=your-email@gmail.com
SITE_URL=http://127.0.0.1:8000

# Database Configuration
# For MySQL (currently configured):
DATABASE_ENGINE=django.db.backends.mysql
DATABASE_NAME=Aniket
DATABASE_USER=root
DATABASE_PASSWORD=tiger
DATABASE_HOST=localhost
DATABASE_PORT=3306

# For SQLite (comment out MySQL config above and uncomment below):
# DATABASE_ENGINE=django.db.backends.sqlite3

